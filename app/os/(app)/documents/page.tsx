import { PlaceholderPage } from "@/components/os/placeholder";

export default function DocumentsPage() {
  return (
    <PlaceholderPage
      title="Documents"
      phase="Phase 1"
      description="Secure, versioned document storage with client/engagement/period metadata, retention rules and short-lived signed access."
    />
  );
}
