import { PlaceholderPage } from "@/components/os/placeholder";

export default function RequestsPage() {
  return (
    <PlaceholderPage
      title="Requests"
      phase="Phase 2"
      description="The ad hoc request inbox — client and internal requests with SLA clocks, triage and scope approval — ships alongside the client portal."
    />
  );
}
